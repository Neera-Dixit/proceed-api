import ACTION from '../common/action_constants';

function route1Reducer(state = {user:{}, load: false},action){
  const tempState = Object.assign({}, state);
  return state;
}
export default route1Reducer;
